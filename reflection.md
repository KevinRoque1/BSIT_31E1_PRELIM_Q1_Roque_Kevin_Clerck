Why did you use inheritance?
- I use inheritance to acquire the properties of another class like vehicle and Iflyable, IDrivable, ITransport, so the vehicles like cars, airplanes and boat can be recognize.
  
Why did you use interfaces?
- I have to use interfaces because i used abstraction and multiple inheritance.
  
Could Helicopter inherit from both Vehicle and Airplane? Why or why not?
- In my case no, since they are both classes, but we can inherit it if one of them is an interface.
  
Why can Helicopter implement both IFlyable and IDriveable?

interface support multiple inheritance so yes since they are both interface i can implement it.
If a Submarine can both sail and dive, how would you design it?

- i would create Submarine cs as a class and i would create an interface for both dive and sail. and I would put both dive and sail in submarine class.
